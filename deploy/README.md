# Deploy (somente VPS)

Esta pasta fica **vazia nesta fase**.

Docker e Docker Compose serão usados **apenas** no deploy na VPS, em uma fase posterior. Não copie estes artefatos para o fluxo de desenvolvimento local e não documente `docker compose up` como forma de rodar o fórum na máquina do desenvolvedor.

Local: PHP, Composer, Node, MySQL e Redis nativos — ver [docs/setup.md](../docs/setup.md).
